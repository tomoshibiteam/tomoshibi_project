rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Replace before production. Backend Functions currently use Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
