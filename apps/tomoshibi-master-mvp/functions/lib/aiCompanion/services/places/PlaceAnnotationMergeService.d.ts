import type { NormalizedPlace, PlaceAnnotation } from "../../types/place";
export declare class PlaceAnnotationMergeService {
    merge(places: NormalizedPlace[], annotations: PlaceAnnotation[]): NormalizedPlace[];
}
